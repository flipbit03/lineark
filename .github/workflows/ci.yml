name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: lint-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: lint-cargo-

      - name: Check formatting
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --workspace -- -D warnings
        env:
          RUSTFLAGS: -Dwarnings

      - name: Doc lints
        run: cargo doc --workspace --all-features --no-deps
        env:
          RUSTDOCFLAGS: -Dwarnings

  test:
    name: Test (async)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: test-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: test-cargo-

      - name: Set up test API token
        if: env.LINEAR_TEST_TOKEN != ''
        run: echo "$LINEAR_TEST_TOKEN" > ~/.linear_api_token_test
        env:
          LINEAR_TEST_TOKEN: ${{ secrets.LINEAR_TEST_TOKEN }}

      - name: Run tests
        run: cargo test --workspace

  test-blocking:
    name: Test (blocking)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: test-blocking-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: test-blocking-cargo-

      - name: Set up test API token
        if: env.LINEAR_TEST_TOKEN != ''
        run: echo "$LINEAR_TEST_TOKEN" > ~/.linear_api_token_test
        env:
          LINEAR_TEST_TOKEN: ${{ secrets.LINEAR_TEST_TOKEN }}

      - name: Run tests
        run: cargo test -p lineark-sdk --features blocking

  build:
    name: Build (${{ matrix.name }})
    needs: lint
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64 (gnu)
            runner: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - name: Linux x86_64 (musl)
            runner: ubuntu-latest
            target: x86_64-unknown-linux-musl
          - name: Linux aarch64 (gnu)
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
          - name: Linux aarch64 (musl)
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-musl
          - name: macOS aarch64
            runner: macos-latest
            target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install musl-tools (Linux only)
        if: contains(matrix.target, 'musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Cache cargo registry & build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ matrix.target }}-cargo-

      - name: Build
        run: cargo build --workspace --target ${{ matrix.target }}
